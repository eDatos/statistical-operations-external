export interface GenericConfig {
    dataset: {
        evolucionElectoralKey,
        metadata,
        data
    };

    visualizer: {
        showHeader,
        showRightsHolder
    };

    metadata: any;

    baseUrl
};
