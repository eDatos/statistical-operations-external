# ===================================================================
# Spring Boot configuration for the "env" profile.
#
# This configuration overrides the application.yml file.
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

spring:


# ===================================================================
# Application specific properties
# Add your own application properties here.
# ===================================================================

application:
    metadata:
        endpoint: FILL_COMMON_METADATA_ENDPOINT #Required: common metadata endpoint
    categoriesSchemes:
        schemePrefix: FILL_CATEGORY_SCHEME_PREFIX
        categories:
            # FILL ARRAY CATEGORIES
            -
                key: FILL_CATEGORY_KEY
                nestedId: FILL_CATEGORY_NESTED_ID #betweem "" for numeric
                iconSrc: FILL_CATEGORY_ICON_SRC
        
# ===================================================================        
# Application debug properties
#
# Uncomment if debug mode needed
# ===================================================================

# debug
